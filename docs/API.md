# 📚 توثيق API | API Documentation

هذا الملف يوثق الدوال والواجهات البرمجية المتاحة في المشروع.

---

## 📋 جدول المحتويات

- [الدوال الرئيسية](#الدوال-الرئيسية)
- [دوال المساعدة](#دوال-المساعدة)
- [المتغيرات العامة](#المتغيرات-العامة)

---

## الدوال الرئيسية

### `compareTexts()`

تقوم بمقارنة النصوص المدخلة وعرض النتائج.

```javascript
compareTexts()
```

**المدخلات:** لا يوجد (تقرأ من حقول الإدخال)

**المخرجات:** تحديث واجهة المستخدم بالنتائج

**مثال:**
```javascript
// يتم استدعاؤها عند الضغط على زر المقارنة
document.getElementById('compareBtn').addEventListener('click', compareTexts);
```

---

### `diffTexts(text1, text2)`

تحسب الفروقات بين نصين.

```javascript
diffTexts(text1, text2)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `text1` | `string` | النص الأول (الأصلي) |
| `text2` | `string` | النص الثاني (المعدل) |

**المخرجات:**
```javascript
{
    diff: Array,      // مصفوفة الفروقات
    added: number,    // عدد الكلمات المضافة
    removed: number,  // عدد الكلمات المحذوفة
    total: number     // إجمالي الكلمات
}
```

**مثال:**
```javascript
const result = diffTexts("مرحبا بالعالم", "مرحبا بالجميع");
console.log(result.added);    // 1
console.log(result.removed);  // 1
```

---

### `detectTextDirection(text)`

تحدد اتجاه النص تلقائياً (RTL أو LTR).

```javascript
detectTextDirection(text)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `text` | `string` | النص المراد تحليله |

**المخرجات:** `'rtl'` أو `'ltr'`

**مثال:**
```javascript
detectTextDirection("مرحبا");  // 'rtl'
detectTextDirection("Hello");  // 'ltr'
```

---

### `renderDiff(diff)`

تحول مصفوفة الفروقات إلى HTML.

```javascript
renderDiff(diff)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `diff` | `Array` | مصفوفة الفروقات |

**المخرجات:** سلسلة HTML

**مثال:**
```javascript
const html = renderDiff([
    { word: "مرحبا", type: "same" },
    { word: "بالعالم", type: "removed" },
    { word: "بالجميع", type: "added" }
]);
```

---

### `switchMode(mode)`

تبديل وضع المقارنة (نصين أو 4 نصوص).

```javascript
switchMode(mode)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `mode` | `number` | `2` لمقارنة نصين، `4` لمقارنة 4 نصوص |

**مثال:**
```javascript
switchMode(2);  // تفعيل وضع مقارنة نصين
switchMode(4);  // تفعيل وضع مقارنة 4 نصوص
```

---

### `switchView(view)`

تبديل طريقة العرض (موحد أو منقسم).

```javascript
switchView(view)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `view` | `string` | `'combined'` للعرض الموحد، `'split'` للعرض المنقسم |

**مثال:**
```javascript
switchView('combined');  // العرض الموحد
switchView('split');     // العرض المنقسم
```

---

## دوال المساعدة

### `lcs(arr1, arr2)`

حساب أطول تتابع مشترك (Longest Common Subsequence).

```javascript
lcs(arr1, arr2)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `arr1` | `Array` | المصفوفة الأولى |
| `arr2` | `Array` | المصفوفة الثانية |

**المخرجات:** مصفوفة العناصر المشتركة مع مواقعها

---

### `getWords(text)`

تحويل النص إلى مصفوفة كلمات.

```javascript
getWords(text)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `text` | `string` | النص |

**المخرجات:** مصفوفة الكلمات

**مثال:**
```javascript
getWords("مرحبا بالعالم");  // ["مرحبا", "بالعالم"]
```

---

### `calculateSimilarity(text1, text2)`

حساب نسبة التشابه بين نصين.

```javascript
calculateSimilarity(text1, text2)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `text1` | `string` | النص الأول |
| `text2` | `string` | النص الثاني |

**المخرجات:** `number` (نسبة مئوية من 0 إلى 100)

**مثال:**
```javascript
calculateSimilarity("مرحبا بالعالم", "مرحبا بالجميع");  // 33
```

---

### `escapeHtml(text)`

تحويل النص لعرضه بأمان في HTML.

```javascript
escapeHtml(text)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `text` | `string` | النص |

**المخرجات:** النص بعد تحويل الأحرف الخاصة

---

### `applyTextDirection(element, text)`

تطبيق اتجاه النص على عنصر HTML.

```javascript
applyTextDirection(element, text)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `element` | `HTMLElement` | عنصر HTML |
| `text` | `string` | النص لتحديد الاتجاه |

---

### `loadExample(lang)`

تحميل مثال توضيحي.

```javascript
loadExample(lang)
```

**المدخلات:**
| المعامل | النوع | الوصف |
|---------|-------|-------|
| `lang` | `string` | `'ar'` للعربية، `'en'` للإنجليزية |

---

### `clearAll()`

مسح جميع الحقول وإخفاء النتائج.

```javascript
clearAll()
```

---

## المتغيرات العامة

| المتغير | النوع | الوصف |
|---------|-------|-------|
| `currentMode` | `number` | الوضع الحالي (2 أو 4) |
| `currentView` | `string` | طريقة العرض الحالية |
| `lastDiffResult` | `Array` | آخر نتيجة مقارنة |
| `lastText1` | `string` | آخر نص أول |
| `lastText2` | `string` | آخر نص ثاني |

---

## أنواع البيانات

### DiffItem

```typescript
interface DiffItem {
    word: string;
    type: 'same' | 'added' | 'removed' | 'changed';
}
```

### DiffResult

```typescript
interface DiffResult {
    diff: DiffItem[];
    added: number;
    removed: number;
    total: number;
}
```
